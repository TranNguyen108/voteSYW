<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hết thời gian bình chọn</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    animation: {
                        'fade-in': 'fadeIn 1s ease-in-out',
                        'clock-tick': 'clockTick 2s ease-in-out infinite',
                    },
                    keyframes: {
                        fadeIn: {
                            '0%': { opacity: '0', transform: 'translateY(20px)' },
                            '100%': { opacity: '1', transform: 'translateY(0)' }
                        },
                        clockTick: {
                            '0%, 100%': { transform: 'rotate(0deg)' },
                            '50%': { transform: 'rotate(10deg)' }
                        }
                    }
                }
            }
        }
    </script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="min-h-screen bg-gradient-to-br from-red-500 via-orange-500 to-yellow-500">
    <div class="min-h-screen flex items-center justify-center px-4">
        <div class="max-w-lg w-full">
            <div class="bg-white/10 backdrop-blur-lg rounded-3xl p-8 border border-white/20 shadow-2xl text-center animate-fade-in">
                <!-- Clock Icon -->
                <div class="mb-8">
                    <i class="fas fa-clock text-8xl text-white animate-clock-tick"></i>
                </div>
                
                <!-- Message -->
                <div class="mb-8">
                    <h1 class="text-4xl md:text-5xl font-bold text-white mb-4">
                        ⏰ Rất tiếc!
                    </h1>
                    <h2 class="text-2xl font-bold text-red-100 mb-6">
                        Đã hết thời gian bình chọn
                    </h2>
                    
                    <div class="bg-white/20 backdrop-blur-lg rounded-2xl p-6 mb-6">
                        <p class="text-lg text-white mb-4">
                            <i class="fas fa-exclamation-triangle mr-2"></i>
                            Phiên bình chọn đã kết thúc
                        </p>
                        <p class="text-red-100">
                            Hệ thống không nhận thêm lượt bình chọn nữa. 
                            <br>Cảm ơn bạn đã quan tâm!
                        </p>
                    </div>
                </div>
                

                
                <!-- Info Note -->
                <div class="pt-6 border-t border-white/20">
                    <p class="text-red-100 text-sm">
                        <i class="fas fa-info-circle mr-1"></i>
                        Vui lòng theo dõi thông báo về các phiên bình chọn tiếp theo.
                    </p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
