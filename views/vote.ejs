<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bình chọn - Chọn đội thi</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="min-h-screen bg-gradient-to-br from-indigo-600 via-purple-600 to-pink-600">
    <div class="min-h-screen py-8 px-4">
        <div class="max-w-6xl mx-auto">
            <!-- Header -->
            <div class="text-center mb-12">
                <div class="flex items-center justify-between mb-8">
                    <a href="/" class="inline-flex items-center bg-white/20 backdrop-blur-lg rounded-full px-6 py-3 text-white hover:bg-white/30 transition-all duration-300">
                        <i class="fas fa-home mr-2"></i>Trang chủ
                    </a>
                    <h1 class="text-4xl md:text-6xl font-bold text-white">
                        🗳️ Chọn đội bình chọn
                    </h1>
                    <div class="w-32"></div>
                </div>
            </div>

            <% if (teams.length === 0) { %>
                <div class="max-w-md mx-auto">
                    <div class="bg-red-500/20 backdrop-blur-lg rounded-3xl p-8 border border-red-300/30 text-center">
                        <i class="fas fa-exclamation-triangle text-6xl text-yellow-300 mb-6"></i>
                        <h2 class="text-2xl font-bold text-white mb-4">Chưa có đội thi nào!</h2>
                        <p class="text-red-100 mb-6">Vui lòng vào trang Admin để thêm đội thi trước khi bắt đầu bình chọn.</p>
                        <a href="/admin" class="inline-block bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 px-8 rounded-full transition-all duration-300 transform hover:scale-105">
                            <i class="fas fa-plus mr-2"></i>Thêm đội thi
                        </a>
                    </div>
                </div>
            <% } else { %>
                <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
                    <% teams.forEach((team, index) => { %>
                        <div class="group">
                            <div class="bg-white/10 backdrop-blur-lg rounded-3xl p-6 border border-white/20 shadow-2xl hover:bg-white/20 hover:scale-105 transition-all duration-300">
                                <div class="text-center">
                                    <!-- Team Number Badge -->
                                    <div class="inline-flex items-center justify-center w-12 h-12 bg-gradient-to-r from-yellow-400 to-orange-500 rounded-full text-white font-bold text-lg mb-4">
                                        <%= index + 1 %>
                                    </div>
                                    
                                    <!-- Team Icon -->
                                    <div class="w-16 h-16 bg-purple-500 rounded-full flex items-center justify-center mx-auto mb-4 group-hover:animate-pulse">
                                        <i class="fas fa-users text-2xl text-white"></i>
                                    </div>
                                    
                                    <!-- Team Info -->
                                    <h3 class="text-xl font-bold text-white mb-2"><%= team.name %></h3>
                                    <% if (team.description) { %>
                                        <p class="text-purple-100 text-sm mb-6"><%= team.description %></p>
                                    <% } else { %>
                                        <div class="mb-6"></div>
                                    <% } %>
                                    
                                    <!-- Vote Button -->
                                    <a href="/vote/<%= team._id %>" class="inline-block bg-gradient-to-r from-green-500 to-emerald-500 hover:from-green-600 hover:to-emerald-600 text-white font-bold py-3 px-6 rounded-full transition-all duration-300 transform hover:scale-105 shadow-lg">
                                        <i class="fas fa-vote-yea mr-2"></i>Bình chọn ngay
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>

                <!-- Instructions -->
                <div class="max-w-2xl mx-auto mt-12">
                    <div class="bg-blue-500/20 backdrop-blur-lg rounded-2xl p-6 border border-blue-300/30">
                        <div class="flex items-center text-blue-100">
                            <i class="fas fa-info-circle text-2xl text-blue-300 mr-4"></i>
                            <div>
                                <h4 class="font-bold text-white">Hướng dẫn:</h4>
                                <p>Nhấn vào nút "Bình chọn ngay" để hiển thị mã QR cho đội thi đó. Khán giả sẽ quét mã QR để bình chọn.</p>
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
