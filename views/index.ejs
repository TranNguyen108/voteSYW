<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bình Chọn</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    animation: {
                        'float': 'float 6s ease-in-out infinite',
                        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
                    },
                    keyframes: {
                        float: {
                            '0%, 100%': { transform: 'translateY(0px)' },
                            '50%': { transform: 'translateY(-20px)' }
                        }
                    }
                }
            }
        }
    </script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="min-h-screen bg-gradient-to-br from-purple-600 via-blue-600 to-teal-500">
    <!-- Background Animation -->
    <div class="absolute inset-0 overflow-hidden">
        <div class="absolute -top-4 -left-4 w-72 h-72 bg-purple-300 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-float"></div>
        <div class="absolute top-1/2 -right-4 w-72 h-72 bg-yellow-300 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-float" style="animation-delay: 2s;"></div>
        <div class="absolute -bottom-8 left-1/2 w-72 h-72 bg-pink-300 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-float" style="animation-delay: 4s;"></div>
    </div>

    <div class="relative min-h-screen flex items-center justify-center px-4">
        <div class="max-w-6xl w-full">
            <!-- Header -->
            <div class="text-center mb-12">
                <h1 class="text-5xl md:text-7xl font-bold text-white mb-6 animate-pulse-slow">
                    🎭 <span class="bg-gradient-to-r from-yellow-400 to-pink-400 bg-clip-text text-transparent">
                        Bình Chọn
                    </span>
                </h1>
                
                <% if (typeof teamCount !== 'undefined' && teamCount > 0) { %>
                    <div class="inline-flex items-center bg-white/20 backdrop-blur-lg rounded-full px-6 py-3 text-white">
                        <i class="fas fa-users mr-2 text-yellow-300"></i>
                        <span class="font-semibold">Hiện có <span class="text-yellow-300 text-xl"><%= teamCount %></span> đội thi</span>
                    </div>
                <% } else { %>
                    <div class="inline-flex items-center bg-red-500/20 backdrop-blur-lg rounded-full px-6 py-3 text-white">
                        <i class="fas fa-exclamation-triangle mr-2 text-yellow-300"></i>
                        <span>Chưa có đội thi nào. Vui lòng vào Admin để thêm đội thi.</span>
                    </div>
                <% } %>
            </div>

            <!-- Main Cards -->
            <div class="grid md:grid-cols-3 gap-8">
                <!-- Admin Card -->
                <div class="group">
                    <div class="bg-white/10 backdrop-blur-lg rounded-3xl p-8 border border-white/20 shadow-2xl hover:bg-white/20 hover:scale-105 transition-all duration-300">
                        <div class="text-center">
                            <div class="w-20 h-20 bg-blue-500 rounded-full flex items-center justify-center mx-auto mb-6 group-hover:animate-bounce">
                                <i class="fas fa-cog text-3xl text-white"></i>
                            </div>
                            <h3 class="text-2xl font-bold text-white mb-4">Quản trị</h3>
                            <p class="text-blue-100 mb-6">Quản lý đội thi và cấu hình hệ thống</p>
                            <a href="/admin" class="inline-block bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 px-8 rounded-full transition-all duration-300 transform hover:scale-105 shadow-lg">
                                <i class="fas fa-user-shield mr-2"></i>Vào Admin
                            </a>
                        </div>
                    </div>
                </div>

                <!-- Voting Card -->
                <div class="group">
                    <div class="bg-white/10 backdrop-blur-lg rounded-3xl p-8 border border-white/20 shadow-2xl hover:bg-white/20 hover:scale-105 transition-all duration-300">
                        <div class="text-center">
                            <div class="w-20 h-20 bg-green-500 rounded-full flex items-center justify-center mx-auto mb-6 group-hover:animate-bounce">
                                <i class="fas fa-vote-yea text-3xl text-white"></i>
                            </div>
                            <h3 class="text-2xl font-bold text-white mb-4">Bình chọn</h3>
                            <p class="text-green-100 mb-6">Bắt đầu phiên bình chọn cho các đội thi</p>
                            <a href="/vote" class="inline-block bg-green-500 hover:bg-green-600 text-white font-bold py-3 px-8 rounded-full transition-all duration-300 transform hover:scale-105 shadow-lg">
                                <i class="fas fa-hand-point-up mr-2"></i>Bình chọn
                            </a>
                        </div>
                    </div>
                </div>

                <!-- Results Card -->
                <div class="group">
                    <div class="bg-white/10 backdrop-blur-lg rounded-3xl p-8 border border-white/20 shadow-2xl hover:bg-white/20 hover:scale-105 transition-all duration-300">
                        <div class="text-center">
                            <div class="w-20 h-20 bg-yellow-500 rounded-full flex items-center justify-center mx-auto mb-6 group-hover:animate-bounce">
                                <i class="fas fa-chart-bar text-3xl text-white"></i>
                            </div>
                            <h3 class="text-2xl font-bold text-white mb-4">Kết quả</h3>
                            <p class="text-yellow-100 mb-6">Xem kết quả bình chọn realtime</p>
                            <a href="/result" class="inline-block bg-yellow-500 hover:bg-yellow-600 text-white font-bold py-3 px-8 rounded-full transition-all duration-300 transform hover:scale-105 shadow-lg">
                                <i class="fas fa-trophy mr-2"></i>Xem kết quả
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Footer -->
            <div class="text-center mt-12">
                <div class="inline-flex items-center bg-white/10 backdrop-blur-lg rounded-full px-6 py-3 text-white/80">
                    <i class="fas fa-heart mr-2 text-red-400"></i>
                    <span>Hệ thống bình chọn hiện đại</span>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
